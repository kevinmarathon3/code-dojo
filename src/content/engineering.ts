import {bi,type Text} from '../types'; import type {Choice} from './labs';
export interface Engineering {id:string;unit:Text;title:Text;prompt:Text;code:string;choices:Choice[];tip:Text;}
const choice=(en:string,es:string,correct:boolean,why:string,whyEs:string):Choice=>({text:bi(en,es),correct,why:bi(why,whyEs)});
export const engineering:Engineering[]=[
{id:'name',unit:bi('Naming & cohesion','Nombres y cohesión'),title:bi('Name the decision','Nombra la decisión'),prompt:bi('This order rule is reused in three places. Which extraction clarifies its intent without changing behavior?','Esta regla se reutiliza en tres lugares. ¿Qué extracción aclara su intención sin cambiar el comportamiento?'),code:'if order.total >= 100 and order.country == "MX":\n    shipping = 0',choices:[choice('qualifies_for_free_shipping(order)','qualifies_for_free_shipping(order)',true,'The name describes the business decision and keeps the condition together.','El nombre describe la decisión de negocio y mantiene unida la condición.'),choice('process(order)','process(order)',false,'Process hides the specific decision being made.','Process oculta la decisión concreta.'),choice('Always set shipping to zero','Pon siempre shipping en cero',false,'That changes the eligibility rule.','Eso cambia la regla de elegibilidad.')],tip:bi('Name a function after the question it answers or the action it performs.','Nombra una función por la pregunta que responde o la acción que realiza.')},
{id:'extract',unit:bi('Naming & cohesion','Nombres y cohesión'),title:bi('Split a tangled function','Divide una función enredada'),prompt:bi('place_order validates quantities, calculates totals, writes SQL, and sends email. Choose a safe first refactor.','place_order valida cantidades, calcula totales, escribe SQL y envía correo. Elige un primer refactor seguro.'),code:'def place_order(order):\n    # validate → calculate → save → email\n    ...',choices:[choice('Extract a pure calculate_total with characterization tests','Extrae calculate_total como función pura con pruebas de caracterización',true,'A pure boundary is easy to verify before changing side effects.','Un límite puro se verifica fácilmente antes de cambiar efectos secundarios.'),choice('Split by line count into four helpers','Divide por número de líneas en cuatro helpers',false,'Line counts do not identify coherent responsibilities.','El número de líneas no identifica responsabilidades coherentes.'),choice('Rewrite everything and change pricing at once','Reescribe todo y cambia precios a la vez',false,'Mixing behavior changes and refactoring makes regressions harder to isolate.','Mezclar cambios de comportamiento y refactoring dificulta aislar regresiones.')],tip:bi('Preserve behavior first; change business policy in a separate step.','Conserva primero el comportamiento; cambia la política de negocio por separado.')},
{id:'modules',unit:bi('Modules & dependencies','Módulos y dependencias'),title:bi('Draw the boundary','Dibuja el límite'),prompt:bi('The order domain imports an HTTP framework solely to throw an HTTP 400 error. How can the domain work from both HTTP and a batch job?','El dominio importa un framework HTTP solo para lanzar un error HTTP 400. ¿Cómo puede funcionar desde HTTP y una tarea batch?'),code:'domain.validate(order)\n  throws HttpBadRequest("quantity")',choices:[choice('Return a domain error; translate it at each entrypoint','Devuelve un error de dominio; tradúcelo en cada entrada',true,'The domain describes the failure without depending on transport.','El dominio describe el fallo sin depender del transporte.'),choice('Use HTTP objects in the batch job too','Usa objetos HTTP también en batch',false,'This spreads the coupling instead of removing it.','Esto extiende el acoplamiento en lugar de eliminarlo.')],tip:bi('Domain concepts should not need to know how requests arrived.','Los conceptos de dominio no necesitan saber cómo llegaron las peticiones.')},
{id:'cycle',unit:bi('Modules & dependencies','Módulos y dependencias'),title:bi('Break a dependency cycle','Rompe un ciclo de dependencias'),prompt:bi('Orders imports Billing, and Billing imports Orders just to read an order summary. Which options can break the cycle?','Orders importa Billing y Billing importa Orders solo para leer un resumen. ¿Qué opciones pueden romper el ciclo?'),code:'Orders → Billing → Orders',choices:[choice('Pass a small immutable order summary to Billing','Pasa a Billing un resumen pequeño e inmutable',true,'Passing the necessary data can remove the reverse import.','Pasar los datos necesarios puede eliminar la importación inversa.'),choice('Define a narrow port owned by the consuming layer','Define un puerto pequeño propiedad de la capa consumidora',true,'A contract can invert the dependency; use it when an abstraction is justified.','Un contrato puede invertir la dependencia; úsalo cuando la abstracción esté justificada.'),choice('Move both imports into startup code without changing calls','Mueve ambos imports al inicio sin cambiar llamadas',false,'Moving import statements may hide rather than remove the conceptual cycle.','Mover imports puede ocultar el ciclo conceptual sin eliminarlo.')],tip:bi('Prefer the smallest boundary that solves the coupling problem.','Prefiere el límite más pequeño que resuelva el acoplamiento.')},
{id:'tests',unit:bi('Testing & refactoring','Pruebas y refactoring'),title:bi('Test the boundary','Prueba el límite'),prompt:bi('Free shipping starts at total >= 100. Which set best protects the threshold?','El envío gratis comienza con total >= 100. ¿Qué conjunto protege mejor el límite?'),code:'free_shipping = total >= 100',choices:[choice('99, 100, 101 with expected shipping outcomes','99, 100, 101 con resultados esperados de envío',true,'Below, at, and above the boundary catch common comparison defects.','Debajo, en y encima del límite detectan errores comunes de comparación.'),choice('Only 1000','Solo 1000',false,'A far-above value does not distinguish > from >=.','Un valor muy superior no distingue > de >=.'),choice('Assert that the source code contains >=','Comprueba que el código contiene >=',false,'That mirrors implementation text rather than behavior.','Eso refleja el texto de implementación en lugar del comportamiento.')],tip:bi('Test observable outcomes and meaningful edge cases.','Prueba resultados observables y casos límite significativos.')},
{id:'regression',unit:bi('Testing & refactoring','Pruebas y refactoring'),title:bi('Refactor with a safety net','Refactoriza con protección'),prompt:bi('A duplicate-coupon bug was fixed manually. What should precede a pricing refactor?','Un error de cupón duplicado se corrigió manualmente. ¿Qué debe preceder un refactor de precios?'),code:'apply_coupon(order, "WELCOME")\napply_coupon(order, "WELCOME")',choices:[choice('Add a regression test for applying the same coupon twice','Añade una prueba de regresión al aplicar dos veces el mismo cupón',true,'The test captures the intended invariant before code moves.','La prueba captura la invariante deseada antes de mover código.'),choice('Assert only that no exception occurs','Comprueba solo que no haya excepciones',false,'The total could be wrong without an exception.','El total podría ser incorrecto sin una excepción.')],tip:bi('Turn a discovered bug into a small durable example of the desired behavior.','Convierte un fallo descubierto en un ejemplo pequeño y durable del comportamiento deseado.')},
{id:'commits',unit:bi('Git & review','Git y revisión'),title:bi('Make a reviewable commit','Crea un commit revisable'),prompt:bi('You renamed pricing helpers and changed tax policy. How should the changes be presented?','Renombraste helpers de precios y cambiaste la política fiscal. ¿Cómo presentar los cambios?'),code:'rename calculate() → subtotal()\nchange tax rate and rounding policy',choices:[choice('Separate behavior-preserving rename from policy change','Separa el renombrado del cambio de política',true,'Reviewers can verify mechanical and behavioral changes independently.','Los revisores pueden verificar cambios mecánicos y de comportamiento por separado.'),choice('One commit called updates','Un commit llamado updates',false,'The purpose and risk of the change are difficult to review.','El propósito y el riesgo son difíciles de revisar.')],tip:bi('A commit should tell one coherent story with relevant validation.','Un commit debe contar una historia coherente con validación relevante.')},
{id:'review',unit:bi('Git & review','Git y revisión'),title:bi('Review the behavior','Revisa el comportamiento'),prompt:bi('A merge conflict changed timeout from 10 to 30 seconds. What must the reviewer establish?','Un conflicto de merge cambió el timeout de 10 a 30 segundos. ¿Qué debe establecer el revisor?'),code:'- timeout = 10\n+ timeout = 30',choices:[choice('The intended service budget and effects on retries','El presupuesto de tiempo del servicio y los efectos en reintentos',true,'A resolved text conflict can still violate a latency contract.','Un conflicto textual resuelto aún puede violar un contrato de latencia.'),choice('Only whether conflict markers were removed','Solo si se eliminaron las marcas de conflicto',false,'Text cleanup does not prove the behavior is correct.','Limpiar el texto no prueba que el comportamiento sea correcto.')],tip:bi('Review why the final behavior is right, not merely whether the patch applies.','Revisa por qué el comportamiento final es correcto, no solo si el parche se aplica.')},
{id:'injection',unit:bi('Layers & interfaces','Capas e interfaces'),title:bi('Replace a dependency in tests','Reemplaza una dependencia en pruebas'),prompt:bi('OrderService creates its database connection internally. Tests need to exercise pricing without a database.','OrderService crea internamente su conexión. Las pruebas necesitan probar precios sin base de datos.'),code:'class OrderService:\n    def place(self, order):\n        db = ProductionDatabase()\n        db.save(order)',choices:[choice('Accept a repository dependency through the constructor','Recibe un repositorio mediante el constructor',true,'Production and test implementations can fulfill the same contract.','Las implementaciones de producción y prueba cumplen el mismo contrato.'),choice('Skip saving whenever a global TEST flag is set','Omite guardar cuando haya una variable global TEST',false,'This introduces special production branches and global state.','Esto introduce ramas especiales y estado global.')],tip:bi('Inject behavior at useful boundaries; do not create an interface for every class automatically.','Inyecta comportamiento en límites útiles; no crees interfaces para todas las clases automáticamente.')},
{id:'architecture',unit:bi('Layers & interfaces','Capas e interfaces'),title:bi('Choose architecture for the constraints','Elige arquitectura según las restricciones'),prompt:bi('Two developers maintain a small order app. They need clear boundaries, one deployment, and simple transactions. Which is the best starting point?','Dos desarrolladores mantienen una app pequeña. Necesitan límites claros, un despliegue y transacciones simples. ¿Cuál es el mejor inicio?'),code:'team = 2\ndeployment = one\ntransactions = local',choices:[choice('A modular monolith with explicit boundaries','Un monolito modular con límites explícitos',true,'It meets these constraints without distributed operational overhead.','Cumple las restricciones sin complejidad operativa distribuida.'),choice('A microservice for every entity','Un microservicio por entidad',false,'Independent services add deployment and consistency costs not required here.','Los servicios independientes añaden costos de despliegue y consistencia innecesarios aquí.')],tip:bi('Architecture is a response to constraints. Different constraints can justify different choices.','La arquitectura responde a restricciones. Otras restricciones pueden justificar otras decisiones.')},
{id:'idempotency',unit:bi('APIs & resilience','APIs y resiliencia'),title:bi('Retry without double charging','Reintenta sin cobrar dos veces'),prompt:bi('A payment request times out after the provider may have charged it. What should a retry reuse?','Una petición de pago agota el tiempo cuando el proveedor quizá ya cobró. ¿Qué debe reutilizar el reintento?'),code:'POST /payments\nnetwork timeout\nretry ?',choices:[choice('The same stable idempotency key for this operation','La misma clave idempotente estable para esta operación',true,'A supporting provider can recognize the operation and return its prior outcome.','Un proveedor compatible puede reconocer la operación y devolver el resultado previo.'),choice('A new random key on every retry','Una clave aleatoria nueva en cada reintento',false,'Each attempt looks like a new payment.','Cada intento parece un pago nuevo.')],tip:bi('Timeout means the outcome is uncertain, not necessarily that the operation failed.','Un timeout significa resultado incierto, no necesariamente que la operación falló.')},
{id:'cache',unit:bi('APIs & resilience','APIs y resiliencia'),title:bi('Bound stale inventory','Limita inventario obsoleto'),prompt:bi('Product browsing may tolerate 30 seconds of stale stock; checkout must prevent overselling. Choose an appropriate boundary.','La navegación tolera 30 segundos de stock obsoleto; el checkout debe evitar sobreventas. Elige un límite apropiado.'),code:'browse → cached stock\ncheckout → ?',choices:[choice('Use cached reads for browsing; reserve stock atomically at checkout','Usa caché al navegar; reserva stock atómicamente en checkout',true,'Different consistency requirements belong to different operations.','Distintas operaciones tienen distintos requisitos de consistencia.'),choice('Trust the browsing cache for every purchase','Confía en la caché de navegación para cada compra',false,'Concurrent purchases can oversell stale stock.','Las compras concurrentes pueden sobrepasar stock obsoleto.')],tip:bi('Choose freshness guarantees per operation, and make them explicit.','Elige garantías de frescura por operación y hazlas explícitas.')}
];
