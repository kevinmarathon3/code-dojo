import {engineering} from './content/engineering';import {Choices} from './LabView';import type {Locale} from './types';import {Lightbulb} from 'lucide-react';
export default function EngineeringView({id,locale,onAttempt}:{id:string;locale:Locale;onAttempt:(correct:boolean)=>void}){const e=engineering.find(x=>x.id===id)??engineering[0];return <div className="lesson-content"><div className="section-kicker">{e.unit[locale]}</div><h2>{e.title[locale]}</h2><p className="lede">{e.prompt[locale]}</p><pre className="trace-code engineering-code">{e.code}</pre><Choices key={id} locale={locale} choices={e.choices} onResult={onAttempt}/><div className="concept-card"><Lightbulb size={20}/><div><span>{locale==='es'?'CONSEJO DE INGENIERÍA':'ENGINEERING TIP'}</span><p>{e.tip[locale]}</p></div></div><p className="model-note">{locale==='es'?'Caso simulado · elige según las restricciones indicadas. Puede haber más de un enfoque válido.':'Simulated case study · choose for the stated constraints. More than one approach may be valid.'}</p></div>;}
